"use client";

// full-viewport CRT scanline overlay — always-on, barely visible
export function ScanLines() {
  return (
    <div
      className="fx-scanlines"
      aria-hidden="true"
    />
  );
}
